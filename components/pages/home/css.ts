import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles(() => ({
    background: {
        background: "#E5E5E5",
        width: "100vw",
        height: "100vh",
    },
}));

export default useStyles